# Rename to avoid container name collisions when running multiple containers
PROJECT_NAME="claude-project"
GITHUB_USERNAME="your_actual_username"
# Set to true to require Artifactory for package downloads
REQUIRE_ARTIFACTORY=false
# Optional: Leave blank if not using Artifactory
ARTIFACTORY_USERNAME="your_artifactory_username"
ARTIFACTORY_TOKEN="your_artifactory_token"
